{
  "name": "aleromano.com",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "astro": "astro",
    "docker:build": "docker build -t aleromano.com .",
    "predeploy": "docker context use hetzner && docker build -t aleromano.com .",
    "deploy": "docker stop $(docker ps -q --filter publish=4321) && docker run -d -p 4321:4321 aleromano.com && docker context use default"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "@astrojs/markdown-remark": "^6.0.2",
    "@astrojs/node": "^9.0.1",
    "@astrojs/rss": "^4.0.11",
    "astro": "^5.1.7",
    "typescript": "^5.5.4"
  }
}
