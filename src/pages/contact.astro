---
import BaseLayout from '../layouts/BaseLayout.astro';

const pageTitle = 'Contact';
export const prerender = true;
---

<BaseLayout pageTitle={pageTitle}>
    <p>Send me a pidgeon</p>
</BaseLayout>
